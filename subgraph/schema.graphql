type Job @entity(immutable: false) {
  id: ID!
  client: Bytes!
  provider: Bytes!
  evaluator: Bytes!
  value: BigInt!
  specHash: String!
  deliverableHash: String
  status: String!
  createdAt: BigInt!
  fundedAt: BigInt
  submittedAt: BigInt
  resolvedAt: BigInt
  expiresAt: BigInt!
  blockNumber: BigInt!
  txHash: Bytes!
}

type Provider @entity(immutable: false) {
  id: ID!
  address: Bytes!
  jobsCompleted: Int!
  jobsRejected: Int!
  jobsExpired: Int!
  totalVolume: BigInt!
  firstJobAt: BigInt!
  lastJobAt: BigInt!
}

type Evaluator @entity(immutable: false) {
  id: ID!
  address: Bytes!
  evaluationsCompleted: Int!
  evaluationsRejected: Int!
  firstEvaluationAt: BigInt!
  lastEvaluationAt: BigInt!
}

type DailyStat @entity(immutable: false) {
  id: ID! # YYYY-MM-DD
  date: String!
  jobsCreated: Int!
  jobsCompleted: Int!
  jobsRejected: Int!
  jobsExpired: Int!
  volumeSettled: BigInt!
}
